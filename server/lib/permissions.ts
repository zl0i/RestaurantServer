

export const enum Resources {
    points = 'points',
    menu = 'menu',
    users = 'users',
    orders = 'orders',
    category = 'category'
}

export enum Actions {
    create = 'create',
    read = 'read',
    update = 'update',
    delete = 'delete'
}

export const enum Scope {
    points = 'points',
    own = 'own'
}